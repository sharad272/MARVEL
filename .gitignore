node_modules/
.next/
out/
build/
dist/

# Secrets — never commit
.env
.env.local
.env*.local

# SQLite database + generated Prisma client
prisma/*.db
prisma/*.db-journal
prisma/dev.db*

# Base64 snapshot of the seeded db, regenerated by `npm run seed` and by
# the Vercel build (see scripts/generate-db-blob.mjs) — never hand-edit.
lib/generated/

# Local media-folder setting (written by Settings when SQLite isn't ready)
.marvelverse.json

# OS / editor
.DS_Store
Thumbs.db
*.log
npm-debug.log*
.vscode/*
!.vscode/extensions.json
.idea/

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Local Vercel project link
.vercel

